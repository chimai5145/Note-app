import stylePage from "../styles/NotePage.module.css";

const NotePageLogOutView = () => {
    return (
        <p className={stylePage.text}> 
        Log in to see note
        </p>
    );
}

export default NotePageLogOutView;